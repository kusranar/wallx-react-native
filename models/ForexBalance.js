export default class ForexBalance{
    constructor(idForexBalance, forexBalance, currency){
        this.idForexBalance = idForexBalance;
        this.forexBalance = forexBalance;
        this.currency = currency;
    }
}